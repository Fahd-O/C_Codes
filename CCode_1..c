#include <stdio.h>

int main()
{
	int poundedYam; //var declaration
	poundedYam = 2;
	//syntax = form format of lang

	int y = poundedYam / 2;
	
	//Expression = anything that can be evaluated to one value, like above
	//statement
	//declaration
	//initialization

	printf();
	return 0;
}
// c is space insensitive.
